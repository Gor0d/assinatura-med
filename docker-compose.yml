services:
  assinatura-med:
    build: .
    ports:
      - "5001:5001"
    env_file:
      - .env
    environment:
      - LOG_DIR=/app/logs
    volumes:
      - logs_data:/app/logs
    restart: unless-stopped

volumes:
  logs_data:
